*.pyc
**/__pycache__
wandb
debug.sh
_temp_videos
anno

# Local-only model weights and benchmark data (pulled via git-lfs, do not commit)
checkpoints/
datasets/
videogen-rewardbench-output/

# wan_eval pipeline artifacts: generated videos, reward CSVs, human annotation
# templates, accuracy_report.json. Code under wan_eval/ is still tracked.
wan_eval/outputs/